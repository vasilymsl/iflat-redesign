---
phase: 08-token-stream-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/tv-token.ts]
autonomous: true
requirements: [TOKEN-01, TOKEN-02, PERF-02]

must_haves:
  truths:
    - "После рестарта сервера tokenIssuedAt восстанавливается из .tv-token-cache.json — proactive refresh корректно вычисляет TTL ratio"
    - "Повторное наведение на тот же канал в течение 60s не создаёт нового API-запроса к stream endpoint"
    - ".tv-token-cache.json содержит поле tokenIssuedAt после получения токена"
  artifacts:
    - path: "src/lib/tv-token.ts"
      provides: "tokenIssuedAt persistence + stream URL cache"
      contains: "tokenIssuedAt"
    - path: ".tv-token-cache.json"
      provides: "Persistent token data with tokenIssuedAt"
      contains: "tokenIssuedAt"
  key_links:
    - from: "savePersistentCache()"
      to: ".tv-token-cache.json"
      via: "fs.writeFileSync with tokenIssuedAt field"
      pattern: "tokenIssuedAt.*cache"
    - from: "loadPersistentCache()"
      to: "cache.tokenIssuedAt"
      via: "data.tokenIssuedAt ?? Date.now() fallback"
      pattern: "tokenIssuedAt.*Date\\.now"
    - from: "getStreamUrl()"
      to: "streamUrlCache Map"
      via: "cache hit check before curlJsonStream"
      pattern: "streamUrlCache.*get"
---

<objective>
Добавить tokenIssuedAt в persistent cache и stream URL кеш 60s в tv-token.ts

Purpose: Proactive refresh работает корректно после рестарта сервера (TOKEN-01/02), повторный hover не бьёт API (PERF-02)
Output: Обновлённый src/lib/tv-token.ts с тремя изменениями
</objective>

<execution_context>
@/Users/vasilymaslovsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vasilymaslovsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-token-stream-hardening/08-CONTEXT.md
@.planning/phases/08-token-stream-hardening/08-RESEARCH.md
@src/lib/tv-token.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: tokenIssuedAt в PersistentTokenData + восстановление при загрузке</name>
  <files>src/lib/tv-token.ts</files>
  <action>
Три изменения в src/lib/tv-token.ts (TOKEN-01, TOKEN-02):

1. **PersistentTokenData interface** — добавить поле `tokenIssuedAt: number`

2. **savePersistentCache()** — добавить `tokenIssuedAt: cache.tokenIssuedAt` в объект, записываемый в JSON-файл

3. **loadPersistentCache()** — при восстановлении из файла: `cache.tokenIssuedAt = data.tokenIssuedAt ?? Date.now()` (fallback для обратной совместимости со старыми файлами без этого поля)

4. **Проверить getGuestToken()** — убедиться что proactive refresh ratio вычисляется как:
   ```
   totalTTL = expiresAt - tokenIssuedAt
   remaining = expiresAt - Date.now()
   ratio = remaining / totalTTL
   ```
   Если ratio вычисляется иначе (например, без tokenIssuedAt) — исправить на эту формулу. Refresh при `ratio < PROACTIVE_REFRESH_RATIO (0.25)`.

Антипаттерн: НЕ хардкодить TTL (90 мин) — вычислять из expiresAt - tokenIssuedAt.
  </action>
  <verify>
    <automated>cd /Users/vasilymaslovsky/Desktop/redesign && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>PersistentTokenData содержит tokenIssuedAt, savePersistentCache записывает его в файл, loadPersistentCache восстанавливает с fallback, proactive refresh ratio использует tokenIssuedAt для вычисления TTL</done>
</task>

<task type="auto">
  <name>Task 2: Stream URL кеш 60s TTL в getStreamUrl()</name>
  <files>src/lib/tv-token.ts</files>
  <action>
Добавить stream URL кеш (PERF-02):

1. **Константа**: `const STREAM_URL_CACHE_TTL_MS = 60_000;`

2. **Тип StreamUrlEntry**: `{ url: string; expiresAt: number }` — добавить в TvTokenCache interface (или рядом с ним) поле `streamUrlCache?: Map<string, StreamUrlEntry>`

3. **В getStreamUrl()** — перед вызовом curlJsonStream добавить cache lookup:
   - Получить globalThis cache через существующий helper
   - Инициализировать `cache.streamUrlCache = cache.streamUrlCache || new Map()`
   - Проверить `cached = cache.streamUrlCache.get(channelId)` — если `cached && cached.expiresAt > Date.now()` → вернуть `cached.url` с логом `[tv-token] Stream URL cache hit: {channelId}`
   - При cache miss: выполнить существующий curlJsonStream, сохранить результат в Map с `expiresAt: Date.now() + STREAM_URL_CACHE_TTL_MS`, залогировать `[tv-token] Stream URL cache miss, fetching: {channelId}`

Антипаттерн: НЕ хранить Map в module-level переменной — только в globalThis (переживает Next.js hot-reload). Использовать существующий globalThis.__tvTokenCache singleton.
  </action>
  <verify>
    <automated>cd /Users/vasilymaslovsky/Desktop/redesign && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Повторный вызов getStreamUrl с тем же channelId в течение 60s возвращает кешированный URL без API-запроса. Логи показывают "cache hit" при повторном hover.</done>
</task>

</tasks>

<verification>
1. `npm run build` — TypeScript компилируется без ошибок
2. `npm run lint` — линтер проходит
3. Запустить dev-сервер, навести на канал дважды — второй раз в логах `Stream URL cache hit`
4. Проверить `.tv-token-cache.json` — поле `tokenIssuedAt` присутствует
</verification>

<success_criteria>
1. `.tv-token-cache.json` содержит поле `tokenIssuedAt` (число) после получения токена
2. Повторное наведение на канал в течение 60s показывает `[tv-token] Stream URL cache hit` в логах — нет нового API-запроса
3. `npm run build` зелёный
4. Proactive refresh ratio вычисляется через `(expiresAt - Date.now()) / (expiresAt - tokenIssuedAt)`
</success_criteria>

<output>
After completion, create `.planning/phases/08-token-stream-hardening/08-01-SUMMARY.md`
</output>
