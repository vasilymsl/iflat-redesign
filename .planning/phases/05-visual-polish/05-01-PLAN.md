---
phase: 05-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/ScrollReveal.tsx
  - src/components/ui/SectionTitle.tsx
  - src/components/sections/FeatureCards.tsx
  - src/app/internet/flat/page.tsx
  - src/app/internet/home/page.tsx
  - src/app/phone/page.tsx
  - src/app/videonablyudenie/page.tsx
autonomous: true
requirements: [VIS-02, VIS-03, VIS-05]

must_haves:
  truths:
    - "Sections fade in from below when scrolling into view"
    - "SectionTitle can display an optional uppercase badge label above the heading"
    - "FeatureCards use flat surface-colored style without borders or shadows"
  artifacts:
    - path: "src/components/ui/ScrollReveal.tsx"
      provides: "Reusable scroll-reveal animation wrapper"
      exports: ["ScrollReveal"]
      min_lines: 15
    - path: "src/components/ui/SectionTitle.tsx"
      provides: "Section title with optional badge prop"
      exports: ["SectionTitle"]
      contains: "badge"
    - path: "src/components/sections/FeatureCards.tsx"
      provides: "Feature cards with flat surface style"
      contains: "bg-brand-surface"
  key_links:
    - from: "src/components/ui/ScrollReveal.tsx"
      to: "framer-motion"
      via: "motion.div with whileInView"
      pattern: "whileInView.*opacity.*1"
    - from: "src/components/ui/SectionTitle.tsx"
      to: "page files"
      via: "badge prop usage"
      pattern: "badge.*="
---

<objective>
Create ScrollReveal animation wrapper, add badge prop to SectionTitle, and update FeatureCards to flat surface style.

Purpose: Elevate visual quality with scroll-triggered entrance animations, contextual section badges, and cohesive card styling that matches the surface-color alternation pattern.
Output: ScrollReveal.tsx component, updated SectionTitle.tsx with badge prop, updated FeatureCards.tsx with flat style.
</objective>

<execution_context>
@/Users/vasilymaslovsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vasilymaslovsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@src/components/ui/SectionTitle.tsx
@src/components/sections/FeatureCards.tsx
@src/app/globals.css

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/components/ui/SectionTitle.tsx (current):
```typescript
interface SectionTitleProps {
  title: string;
  subtitle?: string;
  centered?: boolean;
  className?: string;
}
```

From src/components/sections/FeatureCards.tsx (current):
```typescript
interface Feature {
  icon?: IconName;
  image?: string;
  title: string;
  description: string;
}
interface FeatureCardsProps {
  features: Feature[];
  columns?: 2 | 3;
}
```

Design tokens in globals.css @theme inline:
```css
--color-brand-primary: #F59500;
--color-brand-primary-light: #FFF3DC;
--color-brand-surface: #F7F8FA;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ScrollReveal wrapper and add badge prop to SectionTitle</name>
  <files>src/components/ui/ScrollReveal.tsx, src/components/ui/SectionTitle.tsx</files>
  <action>
1. Create `src/components/ui/ScrollReveal.tsx`:
   - "use client" component
   - Import `motion` from "framer-motion"
   - Props: `children: React.ReactNode`, `delay?: number` (default 0), `className?: string`
   - Render `motion.div` with:
     - `initial={{ opacity: 0, y: 24 }}`
     - `whileInView={{ opacity: 1, y: 0 }}`
     - `viewport={{ once: true, margin: "-50px" }}`
     - `transition={{ duration: 0.5, delay, ease: "easeOut" }}`
   - Pass through className

2. Update `src/components/ui/SectionTitle.tsx`:
   - Add `badge?: string` to `SectionTitleProps` interface
   - Before the `h2` element, if `badge` is provided, render:
     ```tsx
     {badge && (
       <span className="inline-block mb-3 px-4 py-1.5 text-xs font-bold uppercase tracking-widest text-brand-primary bg-brand-primary-light rounded-full">
         {badge}
       </span>
     )}
     ```
   - Keep the component as a server component (no "use client" needed — badge is just a string prop)
   - Preserve all existing props and behavior exactly as-is
  </action>
  <verify>
    <automated>cd /Users/vasilymaslovsky/Desktop/redesign/iflat-redesign && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>ScrollReveal.tsx exists with motion.div whileInView animation. SectionTitle accepts optional badge prop and renders pill-shaped label when provided. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update FeatureCards to flat surface style and apply badges to key pages</name>
  <files>src/components/sections/FeatureCards.tsx, src/app/internet/flat/page.tsx, src/app/internet/home/page.tsx, src/app/phone/page.tsx, src/app/videonablyudenie/page.tsx</files>
  <action>
1. Update `src/components/sections/FeatureCards.tsx`:
   - Change card className from `"text-center p-8 rounded-2xl bg-white border border-gray-100 shadow-sm hover:shadow-md transition-shadow"` to `"text-center p-8 rounded-2xl bg-brand-surface hover:bg-gray-100 transition-colors"`
   - This removes border and shadow, uses flat surface color
   - Keep all other markup (icon container, title, description) exactly the same

2. Add badge props to SectionTitle usage on key pages. For each page, import SectionTitle if not already imported, and add `badge` to the most relevant SectionTitle call:

   - `src/app/internet/flat/page.tsx`: Find the FeatureCards section's SectionTitle (or parent section heading) — if FeatureCards is used directly without SectionTitle wrapper, no badge needed there. Look for existing SectionTitle calls and add `badge="Internet"` to the tariff-related heading.

   - `src/app/phone/page.tsx`: Add `badge="Telephony"` (or "Телефония") to the main SectionTitle on that page — specifically line ~62 `<SectionTitle title="Преимущества IP-телефонии..."` add `badge="Телефония"`

   - `src/app/videonablyudenie/page.tsx`: Add `badge="Видеонаблюдение"` to the first main SectionTitle call (line ~156 `title="Преимущества облачного видеонаблюдения"`)

   NOTE: Only add badges to 2-3 key SectionTitle usages, NOT every one. Over-use dilutes the effect. Choose the most prominent section heading per page.

3. Run `npm run build` to verify everything compiles and renders correctly.
  </action>
  <verify>
    <automated>cd /Users/vasilymaslovsky/Desktop/redesign/iflat-redesign && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>FeatureCards render with flat bg-brand-surface style (no borders, no shadows). 2-3 key SectionTitles display badge pills. Build passes without errors.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes without errors
2. FeatureCards on /internet/flat, /phone, /videonablyudenie show flat surface-colored cards
3. SectionTitle badge prop renders pill above heading when provided
4. ScrollReveal.tsx exports properly and can be imported
</verification>

<success_criteria>
- ScrollReveal.tsx exists and exports a working Framer Motion wrapper
- SectionTitle accepts badge prop and renders uppercase pill label
- FeatureCards use bg-brand-surface flat style without borders/shadows
- Build succeeds with no TypeScript or lint errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-visual-polish/05-01-SUMMARY.md`
</output>
