---
phase: 05-visual-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/CTABanner.tsx
  - src/components/sections/ConnectionSection.tsx
  - src/components/layout/Footer/Footer.tsx
autonomous: true
requirements: [VIS-04, VIS-05]

must_haves:
  truths:
    - "CTABanner displays two-column layout with text+CTA on left and phone number on right"
    - "CTABanner has a subtle dot-grid overlay and pulsing phone icon"
    - "ConnectionSection shows trust bullets on left and form on right (desktop)"
    - "Footer contacts column includes Google Play and App Store download buttons"
  artifacts:
    - path: "src/components/sections/CTABanner.tsx"
      provides: "Enhanced CTA banner with two-column layout and visual polish"
      contains: "grid"
      min_lines: 25
    - path: "src/components/sections/ConnectionSection.tsx"
      provides: "Two-column connection section with trust bullets"
      contains: "lg:grid-cols-2"
      min_lines: 30
    - path: "src/components/layout/Footer/Footer.tsx"
      provides: "Footer with app download buttons"
      contains: "google-play"
  key_links:
    - from: "src/components/sections/CTABanner.tsx"
      to: "src/config/site.ts"
      via: "siteConfig.phoneFree import"
      pattern: "siteConfig\\.phoneFree"
    - from: "src/components/sections/ConnectionSection.tsx"
      to: "src/components/forms/ConnectionForm.tsx"
      via: "ConnectionForm import"
      pattern: "ConnectionForm"
    - from: "src/components/layout/Footer/Footer.tsx"
      to: "src/config/site.ts"
      via: "siteConfig.apps import"
      pattern: "siteConfig\\.apps"
---

<objective>
Upgrade CTABanner with two-column layout and visual enhancements, redesign ConnectionSection with trust-building left column, and add app store download buttons to Footer.

Purpose: Increase conversion potential through trust signals in the connection section, improved CTA banner with clear phone number visibility, and mobile app discoverability in the footer.
Output: Updated CTABanner.tsx, ConnectionSection.tsx, and Footer.tsx.
</objective>

<execution_context>
@/Users/vasilymaslovsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vasilymaslovsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@src/components/sections/CTABanner.tsx
@src/components/sections/ConnectionSection.tsx
@src/components/layout/Footer/Footer.tsx
@src/config/site.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/config/site.ts:
```typescript
export const siteConfig = {
  name: "iFlat",
  phone: "8 (495) 792-59-88",
  phoneHref: "tel:+74957925988",
  phoneFree: "8 (800) 100-59-88",
  phoneFreeHref: "tel:+78001005988",
  email: "info@iflat.ru",
  apps: {
    googlePlay: "https://play.google.com/store/apps/details?id=ru.iflat.mlk",
    appStore: "https://www.apple.com/ru/app-store/",
  },
  // ...
} as const;
```

From src/components/forms/ConnectionForm.tsx (imported by ConnectionSection):
- Exports `ConnectionForm` component (react-hook-form + zod, "use client")

App store badge images exist at:
- `/images/app/app-store.png`
- `/images/app/google-play.png`

Design rules:
- Orange (#F59500) is allowed as CTA banner section background
- Lucide React icons: Phone, Shield, Lock, ClipboardList, Wrench, Wifi available
- Body font-weight: 450, Inter font
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade CTABanner with two-column layout, dot-grid overlay, and pulsing phone icon</name>
  <files>src/components/sections/CTABanner.tsx</files>
  <action>
Rewrite `src/components/sections/CTABanner.tsx` to a two-column design:

1. Keep the `bg-brand-primary` section background.
2. Add a dot-grid overlay (same pattern as HeroBanner but with `opacity-[0.05]`):
   ```tsx
   <div
     className="absolute inset-0 opacity-[0.05]"
     style={{
       backgroundImage: "radial-gradient(circle at 1px 1px, white 1px, transparent 0)",
       backgroundSize: "40px 40px",
     }}
   />
   ```
3. Add `relative overflow-hidden` to the section element.
4. Layout: `grid grid-cols-1 lg:grid-cols-2 gap-8 items-center` inside the container.
5. Left column: heading "Готовы подключиться?" + subtitle "Оставьте заявку или позвоните — подключим за 2-3 дня" + CTA button linking to `#connection` (white text on semi-transparent white background: `bg-white/20 hover:bg-white/30 text-white font-bold px-8 py-3 rounded-full transition-colors`).
6. Right column: phone number block with `text-center lg:text-right`:
   - Small label "Бесплатная горячая линия" in `text-white/70 text-sm mb-2`
   - Phone number `siteConfig.phoneFree` as a link, styled `text-3xl lg:text-4xl font-black hover:opacity-90 transition-opacity`
   - Pulsing phone icon before the number: `<Phone className="w-6 h-6 animate-pulse inline-block mr-2" />`
   - Below: "Звонок бесплатный, 24/7" in `text-white/60 text-sm mt-1`
7. Import `Phone` from "lucide-react", `siteConfig` from "@/config/site".
8. The component remains a server component (no "use client" needed — Phone icon is from lucide-react which works in server components, no state needed).

Mobile: single column, centered text. Desktop: two columns side by side.
  </action>
  <verify>
    <automated>cd /Users/vasilymaslovsky/Desktop/redesign/iflat-redesign && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>CTABanner renders two-column layout with dot-grid overlay, CTA button on left, phone block with pulsing icon on right. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade ConnectionSection to two-column layout with trust bullets, and add app buttons to Footer</name>
  <files>src/components/sections/ConnectionSection.tsx, src/components/layout/Footer/Footer.tsx</files>
  <action>
1. Rewrite `src/components/sections/ConnectionSection.tsx` to two-column layout:

   - Keep the `id="connection"` and `py-20 lg:py-28` padding.
   - Remove `max-w-2xl mx-auto` wrapper — use full container width.
   - Desktop layout: `grid grid-cols-1 lg:grid-cols-2 gap-12 items-start`
   - LEFT column (trust information):
     - SectionTitle with `title="Оставьте заявку на подключение"`, `centered={false}`, `className="mb-6 lg:mb-8"`
     - Subtitle paragraph: "Заполните форму, и наш менеджер свяжется с вами в ближайшее время" in `text-text-secondary mb-8`
     - Three trust bullets as a `space-y-4` list, each with:
       - `<div className="flex items-start gap-4">`
       - Icon in a circle: `<div className="w-10 h-10 rounded-full bg-brand-primary-light flex items-center justify-center flex-shrink-0"><Phone className="w-5 h-5 text-brand-primary" /></div>`
       - Text block: `<div><p className="font-semibold text-text-primary">Title</p><p className="text-sm text-text-secondary">Description</p></div>`
     - Trust items:
       1. Phone icon: "Перезвоним за 5 минут" / "Оставьте номер и мы свяжемся"
       2. Shield icon: "Бесплатный выезд мастера" / "Подключение в удобное для вас время"
       3. Lock icon: "Без скрытых платежей" / "Прозрачные условия и понятные тарифы"
     - Below trust bullets: phone number `<a href={siteConfig.phoneHref} className="inline-flex items-center gap-2 mt-6 text-lg font-semibold text-brand-primary hover:text-brand-primary-hover transition-colors"><Phone className="w-5 h-5" />{siteConfig.phone}</a>`
   - RIGHT column: the form card as-is:
     ```tsx
     <div className="bg-white rounded-2xl shadow-lg border border-gray-100 p-5 sm:p-8">
       <ConnectionForm />
     </div>
     ```
   - Imports: add `Phone, Shield, Lock` from "lucide-react", add `siteConfig` from "@/config/site"
   - Mobile: stacks vertically (trust info on top, form below)

2. Update `src/components/layout/Footer/Footer.tsx`:

   - In the contacts column (the last `<div>` in the grid), after the `<SocialLinks />` block (after `<div className="mt-6"><SocialLinks /></div>`), add:
     ```tsx
     <div className="mt-6">
       <p className="text-sm text-gray-400 mb-3">Скачайте приложение</p>
       <div className="flex gap-3">
         <a
           href={siteConfig.apps.googlePlay}
           target="_blank"
           rel="noopener noreferrer"
         >
           <Image
             src="/images/app/google-play.png"
             alt="Google Play"
             width={130}
             height={40}
             className="h-10 w-auto opacity-80 hover:opacity-100 transition-opacity"
           />
         </a>
         <a
           href={siteConfig.apps.appStore}
           target="_blank"
           rel="noopener noreferrer"
         >
           <Image
             src="/images/app/app-store.png"
             alt="App Store"
             width={130}
             height={40}
             className="h-10 w-auto opacity-80 hover:opacity-100 transition-opacity"
           />
         </a>
       </div>
     </div>
     ```
   - `Image` is already imported in Footer.tsx. `siteConfig` is already imported.

3. Run `npm run build` to verify everything compiles.
  </action>
  <verify>
    <automated>cd /Users/vasilymaslovsky/Desktop/redesign/iflat-redesign && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>ConnectionSection displays two-column layout with 3 trust bullets on left and form on right. Footer shows Google Play and App Store download buttons below social links. Build passes without errors.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes without errors
2. CTABanner shows two-column layout with dot-grid, CTA button, and pulsing phone icon
3. ConnectionSection shows trust bullets left, form right on desktop; stacked on mobile
4. Footer contacts column has app store download buttons with hover opacity effect
</verification>

<success_criteria>
- CTABanner has two-column layout, dot-grid overlay, pulsing phone icon
- ConnectionSection has trust bullets (phone, shield, lock) on left, form on right
- Footer displays Google Play and App Store buttons in contacts column
- All changes visible on /tv, /internet/flat, /phone, /business/internet pages that use these components
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-visual-polish/05-02-SUMMARY.md`
</output>
